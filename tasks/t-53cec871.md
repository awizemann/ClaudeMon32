---
id: t-53cec871
title: P4.5 — Autonomy cutover (on-device poll loop, Mac-optional, NVS persistence)
status: todo
added: 2026-07-13
---

## Description

Wire the on-device fetchers + OAuth + render into a self-driving poll loop (fetch → build Dashboard → ui_update → 1Hz tick), replacing the host push as the primary driver while keeping the host TCP push as an optional override. Persist last-good state + config + tokens in NVS so the panel survives reboot without a Mac. Respect the same refresh cadence + rate-limit backoff as the host daemon.

## Plan



## Artifacts



