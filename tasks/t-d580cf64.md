---
id: t-d580cf64
title: P4.1 — WiFi transport (device TCP server + mDNS; host TCP push)
status: done
added: 2026-07-13
---

## Description

Device runs a TCP server (port ~8781) speaking the same newline-JSON protocol as serial, dispatched through Protocol::handle with the reply written to the client (refactor handleLine to take a Print& sink; Serial and WiFiClient both are Print). Advertise mDNS claudemon.local. Host: add a TCP transport to DeviceLink (or a NetworkLink) so `claudemon run` can push set_cockpit over TCP (discover via mDNS or --device tcp://host:port); serial stays fully working. Test: unplug USB, dashboard still updates over WiFi.

## Plan



## Artifacts



