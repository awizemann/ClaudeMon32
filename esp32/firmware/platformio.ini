; PlatformIO Project Configuration File
; Waveshare ESP32-S3 1.54" e-Paper AIoT Board

[env:esp32-s3-devkitc-1]
platform = espressif32
board = esp32-s3-devkitc-1
framework = arduino

; USB-CDC for serial output and upload
build_flags =
    -DARDUINO_USB_MODE=1
    -DARDUINO_USB_CDC_ON_BOOT=1
    -DBOARD_HAS_PSRAM
    -mfix-esp32-psram-cache-issue

; Upload via USB-Serial/JTAG. The port name is assigned by macOS and can
; change across reconnects; override per-invocation with --upload-port if it
; drifts (or omit these lines to let PlatformIO auto-detect a single device).
upload_port = /dev/cu.usbmodem201301
upload_speed = 921600
monitor_port = /dev/cu.usbmodem201301
monitor_speed = 115200

; Libraries
lib_deps =
    h2zero/NimBLE-Arduino@^2.3.0
    bblanchon/ArduinoJson@^7.3.0
    Wire

; Board-specific settings
board_build.arduino.memory_type = qio_opi
board_build.flash_size = 8MB
board_build.psram_type = opi
board_upload.flash_size = 8MB
